import QtQuick
import QtQuick.Controls

ApplicationWindow {
    id: win
    visible: true
    width: 900
    height: 700
    title: "omapic"

    Label {
        anchors.centerIn: parent
        text: "omapic — open an image"
        color: "white"
    }

    color: "#1c1c1e"
}
